import { PipenewcompoPipe } from './pipenewcompo.pipe';

describe('PipenewcompoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipenewcompoPipe();
    expect(pipe).toBeTruthy();
  });
});
